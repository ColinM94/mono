{
  "name": "root",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "atlas:": "pnpm --filter atlas",
    "avalon:": "pnpm --filter avalon",
    "beer:": "pnpm --filter beer",
    "ui:": "pnpm --filter @mono/ui",
    "firebase:": "pnpm --filter firebase",
    "build": "pnpm -r build",
    "lint": "pnpm atlas: lint && pnpm avalon: lint && pnpm firebase: lint"
  },
  "devDependencies": {
    "@rolldown/plugin-babel": "^0.2.3",
    "@vitejs/plugin-react": "^6.0.5",
    "oxfmt": "^0.64.0",
    "oxlint": "^1.77.0",
    "vite": "^8.1.5",
    "vite-bundle-analyzer": "^1.3.9"
  }
}
