{
  "name": "root",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "atlas:b": "pnpm --filter atlas ",
    "build": "pnpm -r build",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "atlas:dev": "pnpm --filter atlas dev",
    "atlas:build": "pnpm --filter atlas build",
    "atlas:visualise": "pnpm --filter atlas visualise",
    "avalon:dev": "pnpm --filter avalon dev",
    "visualise": "pnpm dlx vite-bundle-visualizer -c vite.config.ts"
  },
  "devDependencies": {
    "oxlint": "^1.77.0",
    "@vitejs/plugin-react": "^6.0.5",
    "vite": "^8.1.5",
    "@rolldown/plugin-babel": "^0.2.3"
  }
}
